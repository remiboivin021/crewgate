{
  "name": "crewgate",
  "version": "0.1.0",
  "description": "Multi-agent pipeline orchestrator for OpenCode — 7-gate autonomous execution",
  "license": "MIT",
  "author": "Remi Boivin",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run build:ts",
    "build:ts": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^6.0.3",
    "vitest": "^3.0.0"
  },
  "repository": {
    "type": "github",
    "url": "https://github.com/remiboivin021/crewgate"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
