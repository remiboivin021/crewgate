package crewgate.routing

# Reference-only v0.1 policy sketch. The executable router remains local and deterministic
# until a real policy runtime is introduced.

default level := 2
default flow := "feature"
